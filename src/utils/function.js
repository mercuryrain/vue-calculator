/**
 * @param {Function} func
 * @returns {boolean}
 */
export const isUnary = (func) => func.length === 1;

/**
 * @param {Function} func
 * @returns {boolean}
 */
export const isBinary = (func) => func.length === 2;
