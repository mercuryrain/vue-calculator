/**
 * @param {*[]} arr
 * @returns {number[]}
 */
export const toNumbers = (arr) => arr.map(Number);

/**
 * @param {*[]} arr
 * @returns {*[]}
 */
export const reverse = (arr) => [...arr].reverse();
